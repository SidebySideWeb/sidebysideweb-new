---
import {phParts} from '../../lib/v2/placeholder'

interface Props {
  text?: string | null
}

const {text} = Astro.props
const parts = phParts(text)
---

{parts.map((part) => (part.placeholder ? <span class="ph">{part.text}</span> : part.text))}
