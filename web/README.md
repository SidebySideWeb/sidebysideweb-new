# Astro front end (`web/`)

See the [repo README](../README.md) for env vars, seed, Studio, and deploy.

```powershell
npm install
npm run dev        # http://localhost:4321
npm run build
npm run preview
npm run check:redirects
```
